package se.iths.java21.arrays;

import se.iths.java21.objects.Box;

public class  KomplexaArrayer {

    public static void main(String[] args) {
        Box[] boxes = new Box[3];

        /* for (int i = 0; i < boxes.length; i++) {
            boxes[i] = new Box();//När vi har komplexa datatyper måste vi först göra detta, vi skapar ett objekt här.
            //if(boxes[i] != null) Det här är ett sätt att kolla så man inte har ett NullPointerException. *Det är inte clean code
            boxes[i].width = 10 * (i +1);

            System.out.println(boxes[i].width);
        }*/
    }
}
